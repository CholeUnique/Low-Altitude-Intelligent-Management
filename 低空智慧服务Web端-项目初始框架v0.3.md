# 低空智慧服务 Web 端系统 — 项目初始框架

> 版本：v0.3（确认林业6节点 + 领域配置驱动后更新）
> 创建日期：2026-09-07
> 状态：待确认后进入开发

---

## 一、项目概述

### 1.1 定位

基于无人机机库的智能化空间数据全链路操作系统，深度融合无人机管控、二三维建模、时空 AI、事件识别与处置能力，构建"感知-处理-应用"一体化的时空数据中枢，为自然资源、交通、公安和应急等领域提供从数据采集到行业应用的闭环解决方案。

### 1.2 目标用户

某市林业局、国土局等政府部门员工。

### 1.3 核心能力

- 无人机任务全生命周期管理（地理底图与航线规划 → 无人机巡航与影像采集 → 多期影像变化监测 → 问题图斑识别 → 核查任务下发 → 复核与归档）
- 二三维地图可视化（天地图底图 + 多图层 + 绘制工具）
- 无人机实时视频直播回传
- 时空 AI 事件识别与告警
- 多领域监管工作台（按领域切换工作流）
- 多角色权限控制

---

## 二、技术栈选型

| 层级 | 技术 | 说明 |
|---|---|---|
| 前端框架 | Vue 3 + Composition API | `<script setup>` 语法 |
| 构建工具 | Vite 5 | 开发热更新，生产构建 |
| 语言 | TypeScript | 类型安全 |
| UI 组件库 | Element Plus | 按需引入，主题可定制 |
| 路由 | Vue Router 4 | 动态路由 + 权限守卫 |
| 状态管理 | Pinia | 用户态、全局配置态 |
| HTTP 请求 | Axios | 拦截器统一处理 |
| 地图 | 天地图 JS API + 封装层 | 2D/3D 切换，图层管理 |
| 视频流 | 待确认（后续由无人机机库/图传系统提供） | 无人机实时回传，协议待定 |
| 实时通信 | WebSocket | 无人机位置、告警推送 |
| 图表 | ECharts 5 | 统计大屏数据可视化 |
| 样式 | SCSS + CSS Variables | 主题变量化，便于换肤 |
| 代码规范 | ESLint + Prettier | 统一格式 |
| 第三方嵌入 | iframe 封装组件 | 预留微前端升级空间 |

---

## 三、目录结构

```
low-altitude-smart-service/
├── public/
│   └── favicon.ico
├── src/
│   ├── api/                    # 接口层
│   │   ├── index.ts            # axios 实例 + 拦截器
│   │   ├── dashboard.ts        # 运行中枢接口
│   │   ├── task.ts             # 任务管理接口
│   │   ├── workspace.ts        # 监管工作台接口
│   │   ├── drone.ts            # 无人机/视频接口
│   │   └── types.ts            # 接口类型定义
│   ├── assets/                 # 静态资源
│   │   ├── images/
│   │   └── icons/
│   ├── components/             # 全局通用组件
│   │   ├── MapContainer/       # 地图容器组件
│   │   ├── VideoPlayer/        # 视频播放组件
│   │   ├── StatCard/           # 统计卡片
│   │   ├── FlowSteps/          # 流程节点条
│   │   ├── IframeEmbed/        # 第三方系统嵌入
│   │   └── PermissionGuard/    # 权限守卫组件
│   ├── layouts/                # 布局组件
│   │   ├── BasicLayout.vue     # 基础布局（侧边栏+顶栏+内容）
│   │   ├── DashboardLayout.vue # 大屏布局（全屏无侧边栏）
│   │   └── WorkspaceLayout.vue # 工作台布局（顶部流程节点+内容）
│   ├── router/
│   │   ├── index.ts            # 路由实例
│   │   ├── routes.ts           # 静态路由表
│   │   └── guard.ts            # 路由守卫（权限校验）
│   ├── stores/                 # Pinia
│   │   ├── user.ts             # 用户信息 + 权限
│   │   ├── app.ts              # 全局配置（主题、侧边栏状态）
│   │   └── websocket.ts        # WS 连接管理
│   ├── styles/
│   │   ├── variables.scss      # 全局 SCSS 变量
│   │   ├── theme.scss          # 主题色定义
│   │   ├── reset.scss          # 样式重置
│   │   └── index.scss          # 样式入口
│   ├── utils/
│   │   ├── auth.ts             # 权限判断工具
│   │   ├── map.ts              # 地图工具函数
│   │   └── format.ts           # 格式化工具
│   ├── views/
│   │   ├── login/
│   │   │   └── index.vue
│   │   ├── dashboard/
│   │   │   └── index.vue       # 低空治理运行中枢
│   │   ├── task/
│   │   │   └── index.vue       # 低空治理任务总览
│   │   └── workspace/
│   │       └── index.vue       # 领域监管工作台（动态领域参数）
│   ├── workspace-components/   # 工作台各领域节点组件（林业6节点，其他领域按配置扩展）
│   │   ├── route-planning/     # 节点1：地理底图与航线规划
│   │   ├── cruise-capture/     # 节点2：无人机巡航与影像采集
│   │   ├── change-detection/   # 节点3：多期影像变化监测
│   │   ├── spot-identification/# 节点4：问题图斑识别
│   │   ├── task-dispatch/      # 节点5：核查任务下发
│   │   └── review-archive/     # 节点6：复核与归档
│   ├── App.vue
│   └── main.ts
├── .env.development             # 开发环境变量
├── .env.production              # 生产环境变量
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── PROJECT_INIT.md              # 本文档
```

---

## 四、路由设计

### 4.1 路由表

| 路径 | 页面 | 布局 | 说明 |
|---|---|---|---|
| `/login` | 登录页 | 无 | 账号密码登录 |
| `/dashboard` | 低空治理运行中枢 | DashboardLayout | 登录后默认首页，大屏概览 |
| `/taskLists` | 低空治理任务总览 | BasicLayout | 任务列表管理 |
| `/workspace/:field` | 领域监管工作台 | WorkspaceLayout | `:field` 为领域标识（如 forestry / land / traffic） |
| `/` | 重定向 → `/dashboard` | — | — |

### 4.2 路由守卫逻辑

```
进入路由 → 是否登录？
  ├─ 否 → 跳转 /login
  └─ 是 → 目标路由是否需要特定权限？
       ├─ 有权限 → 进入
       └─ 无权限 → 跳转 /dashboard 并提示"无访问权限"
```

---

## 五、页面与组件规划

### 5.1 页面 1：低空治理运行中枢（`/dashboard`）

**布局参考**：深色科技风大屏（见参考图）

**页面结构**：

```
┌─────────────────────────────────────────────────────────┐
│  顶栏：Logo + 页面标题 + 系统状态 + C1 用户切换          │
├────────┬────────┬────────┬──────────────────────────────┤
│ 统计卡1 │ 统计卡2 │ 统计卡3 │ 统计卡4（在线设备/今日任务/  │
│        │        │        │ 待研判/闭环完成率）            │
├──────────────────────────────┬───────────────────────────┤
│                              │  任务列表（滚动）           │
│        中央地图区域            │  - 任务名 + 状态标签       │
│  - 左侧图层/设备/事件/测量    │  - 航线 + 无人机编号        │
│    工具栏                     │  - "查看全部任务"→跳转      │
│  - 无人机实时位置 + 航线      │    /taskLists              │
│  - 异常事件标注               │                           │
│  - 2D/3D 切换 + 底图选择     │                           │
├──────────────┬───────────────┼───────────────────────────┤
│ C2 实时视频   │ AI 识别结果    │  工单流转进度               │
│ 回传窗口      │ （截图+框选）  │  （步骤条+当前节点）        │
├──────────────┴───────────────┴───────────────────────────┤
│  底部流程节点条：任务创建→航线执行→视频回传→AI识别→工单派发→复核归档 │
└───────────────────────────────────────────────────────────┘
```

**本页组件清单**：

| 编号 | 组件名 | 说明 |
|---|---|---|
| C1 | UserSwitchModal | 用户切换登录弹窗 |
| C2 | DroneVideoWindow | 无人机实时视频回传窗口（支持多画面切换） |
| C3 | StatCard | 统计数据卡片（可复用，4个实例） |
| C4 | MapDashboard | 大屏地图组件（含图层工具栏、无人机轨迹、事件标注） |
| C5 | TaskListPanel | 任务列表面板 |
| C6 | AiIdentifyPanel | AI 识别结果展示面板 |
| C7 | WorkflowProgress | 工单流转进度组件 |
| C8 | FlowNodeBar | 底部流程节点条（全局复用） |

---

### 5.2 页面 2：低空治理任务总览（`/taskLists`）

**页面结构**：

```
┌─────────────────────────────────────────────────────────┐
│  BasicLayout 顶栏 + 侧边栏                                │
├─────────────────────────────────────────────────────────┤
│  页面 Header：                                            │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 标题"任务总览"  搜索框  筛选栏(领域/状态/时间)   │   │
│  │                    [+ 新增任务]                    │   │
│  └──────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  任务列表（表格 / 卡片流）：                              │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 任务名 | 所属领域 | 创建时间 | 当前流程节点 | 状态 │   │
│  │ ─────────────────────────────────────────────── │   │
│  │ XX林区巡查 | 林业 | 09-07 | AI识别中 | 进行中    │   │
│  │ ...点击行 → 跳转 /workspace/:field（带任务ID）   │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**本页组件清单**：

| 编号 | 组件名 | 说明 |
|---|---|---|
| C9 | TaskFilterBar | 任务筛选栏（搜索 + 多条件筛选） |
| C10 | TaskTable | 任务列表表格（含状态标签、流程节点展示） |
| C11 | TaskCreateModal | 新增任务弹窗（表单：任务名、领域、航线、时间等） |
| C12 | TaskBatchActionBar | 批量操作栏（删除等） |

---

### 5.3 页面 3：领域监管工作台（`/workspace/:field`）

**核心机制**：根据 `:field` 动态加载对应领域的流程节点配置，每个节点对应一个独立组件页面。工作台为**浅色主题**（与 Dashboard 深色大屏区分），标准后台管理风格。

**参考图**：林业执法监管工作台（见已上传参考图2）

**页面结构**：

```
┌─────────────────────────────────────────────────────────────────────┐
│  WorkspaceLayout 顶栏（深蓝色 #003561）：                            │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │ LOGO | 「领域」执法监管工作台   7节点流程条    今日任务/待办/用户│  │
│  │ [地理底图  →无人机巡航 →多期影像 →问题图斑 →核查下发 →现场核查 →复核归档] │  │
│  └───────────────────────────────────────────────────────────────┘  │
├──────────┬──────────────────────────────────────┬───────────────────┤
│ 左侧工具  │           地图作业区                    │  右侧信息面板      │
│ 面板      │  ┌────────────────────────────────┐  │  ┌─────────────┐ │
│ (可折叠)  │  │  天地图卫星影像底图               │  │  │ 巡查任务     │ │
│          │  │  - 巡查区域多边形(半透明蓝)        │  │  │  任务名/区域/ │ │
│ ▸底图与图层│  │  - 航线(蓝色实线)                 │  │  │  无人机/飞手/ │ │
│  ☑卫星影像 │  │  - 航点(蓝色圆点+编号)            │  │  │  计划时间     │ │
│  ☐电子地图 │  │  - 起降点(绿色H标记)              │  │  ├─────────────┤ │
│  ☑林班小班 │  │  - 标注标签(巡查区域/航线/航点)   │  │  │ 航线参数     │ │
│  ☑林地范围 │  │                                │  │  │ 高度/航速/航向/│ │
│  ☐审批项目 │  │  右侧浮动工具:                    │  │  │ 重叠率/安全高/│ │
│  ☑保护区   │  │  [指南针][定位][+][-][图层]      │  │  │ 返航点       │ │
│  ☐历史图斑 │  │  [测量][标注][全屏]              │  │  ├─────────────┤ │
│  ☑机场/起降│  │  底部: 比例尺 0 500 1000m       │  │  │ 规划结果     │ │
│  ☑禁飞区   │  └────────────────────────────────┘  │  │ 航点数/长度/  │ │
│          │                                        │  │ 时长/面积/照片 │ │
│ ▸巡查区域  │                                        │  │ 数           │ │
│  [搜索][框选]│                                        │  └─────────────┘ │
│  [绘制][导入]│                                        │                   │
│          │                                        │  底部操作按钮:      │
│ ▸航线规划  │                                        │  [调用低空大师规划]│
│  [新建][自动]│                                        │  [保存草稿]       │
│  [编辑][清空]│                                        │  [下发任务]       │
├──────────┴──────────────────────────────────────┴───────────────────┤
│  底部三栏（可折叠）：                                                   │
│  ┌──────────────┬──────────────────┬──────────────────────────────┐ │
│  │ 航点列表      │ 历史航线/任务      │ 规划说明 / 告警               │ │
│  │ 编号|经|纬|高 │ 任务名|区域|日期|  │ 🔴限飞提醒                   │ │
│  │ 1|...|...|120│ 航点数|状态        │ 🟠覆盖率提示                  │ │
│  │ 2|...|...|120│ ...更多>           │ 🟠航线冲突提示                │ │
│  └──────────────┴──────────────────┴──────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

> **注意**：以上为节点1"地理底图与航线规划"的详细布局。其他节点切换时，中间内容区和左右面板会随节点功能变化，但整体三栏 + 顶栏 + 底部的框架保持一致。

**林业领域 6 个流程节点（参考图已确认，已去掉"现场核查与整改"）**：

| 序号 | 节点名 | 英文标识 | 说明 |
|---|---|---|---|
| 1 | 地理底图与航线规划 | route-planning | 图层管理、区域绘制、航线规划、航点编辑 |
| 2 | 无人机巡航与影像采集 | cruise-capture | 无人机执行监控、实时视频、影像采集状态 |
| 3 | 多期影像变化监测 | change-detection | 多期影像对比、变化区域检测、差异分析 |
| 4 | 问题图斑识别 | spot-identification | AI识别结果、图斑列表、置信度、人工复核 |
| 5 | 核查任务下发 | task-dispatch | 问题转核查任务、派发对象、下发记录 |
| 6 | 复核与归档 | review-archive | 最终复核、资料归档、闭环确认 |

**领域与节点映射（各领域节点数不固定，配置驱动）**：

| 领域 field | 节点数 | 节点配置 | 说明 |
|---|---|---|---|
| 林业 forestry | 6 | 如上表 | 参考图已确认 |
| 国土 land | 待定 | 待定义（可能复用林业6节点，名称微调） | 如"问题图斑识别"→"违建图斑识别"等 |
| 交通 traffic | 待定 | 待定义 | |
| 应急 emergency | 待定 | 待定义 | |

> **设计原则**：各领域节点数量和名称不固定，通过领域配置文件（`workspace/config/`）动态驱动流程条渲染和组件加载。节点组件库预置通用组件，领域可复用或扩展。新增领域只需新增配置 + 按需新增定制组件，不需要修改框架代码。

**工作台节点组件清单（6个林业节点组件 + 工作台通用组件）**：

| 编号 | 组件目录 | 节点名 | 核心功能 |
|---|---|---|---|
| C13 | route-planning/ | 地理底图与航线规划 | 图层勾选面板、区域绘制/导入、航线新建/自动生成、航点编辑、规划结果统计、告警提示 |
| C14 | cruise-capture/ | 无人机巡航与影像采集 | 无人机实时状态、飞行轨迹、视频回传、影像采集进度、异常告警 |
| C15 | change-detection/ | 多期影像变化监测 | 多期影像选择、对比滑块、变化区域高亮、变化统计、差异导出 |
| C16 | spot-identification/ | 问题图斑识别 | AI识别图斑列表、地图标注、置信度、类型筛选、人工确认/驳回 |
| C17 | task-dispatch/ | 核查任务下发 | 图斑转任务、派发对象选择、任务表单、下发记录、批量操作 |
| C18 | review-archive/ | 复核与归档 | 全流程数据复核、归档操作、闭环确认、档案导出 |

**工作台通用组件（跨节点复用）**：

| 编号 | 组件名 | 说明 |
|---|---|---|
| C19 | WorkspaceHeader | 工作台顶栏（Logo + 领域标题 + 动态流程条 + 用户区） |
| C20 | WorkspaceFlowSteps | 流程节点步骤条（配置驱动，可点击切换，显示当前/已完成/未开始状态） |
| C21 | LayerPanel | 图层列表面板（勾选显隐 + 更多操作） |
| C22 | MapWorkspace | 工作台地图组件（含浮动工具栏、比例尺、标注） |
| C23 | InfoPanel | 右侧信息面板（可折叠，分区展示任务/参数/结果） |
| C24 | WaypointTable | 航点列表表格 |
| C25 | AlertPanel | 规划说明/告警面板（分级展示：🔴严重 🟠警告） |

---

## 六、全局样式与设计系统

### 6.1 配色方案

以 `#003561` 为主色，系统采用**双主题**：Dashboard 运行中枢为深色大屏主题，任务总览与监管工作台为浅色标准后台主题。

```scss
// ========== 主色系（两套主题共用） ==========
$color-primary: #003561;          // 主色（深蓝）
$color-primary-light: #00509e;    // 主色-亮
$color-primary-lighter: #1a6bc4;  // 主色-更亮
$color-primary-dark: #002847;     // 主色-暗
$color-primary-darker: #001a30;   // 主色-更暗

// ========== 功能色（两套主题共用） ==========
$color-success: #00c48c;            // 成功/在线/已完成
$color-warning: #ff9f43;            // 警告/待研判
$color-danger: #ff4d6a;             // 危险/异常/高风险
$color-info: #3da5ff;               // 信息/进行中

// ========== 数据可视化色板（两套主题共用） ==========
$chart-colors: (
  #3da5ff,   // 蓝
  #00c48c,   // 绿
  #ff9f43,   // 橙
  #ff4d6a,   // 红
  #a855f7,   // 紫
  #fbbf24,   // 黄
);

// ==================================================
// 主题 A：深色大屏主题（Dashboard 运行中枢）
// ==================================================
$dark-bg-page: #0a1628;                 // 页面底色
$dark-bg-card: rgba(0, 53, 97, 0.3);   // 卡片背景（半透明主色）
$dark-bg-card-solid: #0d2137;           // 卡片背景（实色）
$dark-bg-header: #001f3f;               // 顶栏背景
$dark-bg-hover: rgba(26, 107, 196, 0.15); // hover 背景

$dark-text-primary: #ffffff;             // 主文字（白）
$dark-text-regular: #c0d4e8;             // 常规文字
$dark-text-secondary: #7a94b0;           // 次要文字
$dark-text-disabled: #4a6078;            // 禁用文字

$dark-border-color: rgba(61, 165, 255, 0.2);
$dark-border-light: rgba(255, 255, 255, 0.08);

// ==================================================
// 主题 B：浅色标准主题（任务总览 / 监管工作台）
// ==================================================
$light-bg-page: #f0f2f5;                 // 页面底色（浅灰）
$light-bg-card: #ffffff;                  // 卡片背景（白）
$light-bg-header: #003561;                // 顶栏背景（主色深蓝）
$light-bg-hover: #f5f7fa;                 // hover 背景
$light-bg-tooltip: #003561;               // 工具提示背景

$light-text-primary: #1f2937;             // 主文字（深灰黑）
$light-text-regular: #4b5563;             // 常规文字
$light-text-secondary: #6b7280;           // 次要文字
$light-text-disabled: #9ca3af;            // 禁用文字
$light-text-on-primary: #ffffff;           // 主色背景上的文字（白）

$light-border-color: #e5e7eb;             // 边框色
$light-border-light: #f3f4f6;             // 浅边框
$light-border-primary: #003561;           // 主色边框

// 浅色主题专用：面板分区标题背景
$light-panel-header-bg: #f8fafc;
$light-panel-title-color: #003561;
```

### 6.2 字体与字号

```scss
$font-family: 'PingFang SC', 'Microsoft YaHei', 'Helvetica Neue', Arial, sans-serif;

$font-size-xs: 12px;
$font-size-sm: 13px;
$font-size-base: 14px;
$font-size-lg: 16px;
$font-size-xl: 18px;
$font-size-2xl: 24px;
$font-size-3xl: 32px;    // 大屏数字
$font-size-4xl: 48px;    // 大屏核心指标
```

### 6.3 间距与圆角

```scss
$spacing-xs: 4px;
$spacing-sm: 8px;
$spacing-md: 16px;
$spacing-lg: 24px;
$spacing-xl: 32px;

$radius-sm: 4px;
$radius-md: 8px;
$radius-lg: 12px;
```

### 6.4 主题使用说明

**深色大屏主题（Dashboard）**：
- 卡片统一使用 `border: 1px solid $dark-border-color` + 半透明背景 + 轻微内发光
- 核心数字使用等宽字体或 `font-variant-numeric: tabular-nums`
- 页面背景可加微弱网格纹理或径向渐变
- 全屏无侧边栏布局

**浅色标准主题（TaskLists / Workspace）**：
- 卡片使用白色背景 + `$light-border-color` 细边框 + 轻微阴影
- 顶栏使用主色 `#003561` 实色背景 + 白色文字
- 面板分区标题使用浅灰背景 `$light-panel-header-bg` + 主色文字
- 按钮主色为 `#003561`，hover 时使用 `$color-primary-light`
- 标准后台布局（BasicLayout：侧边栏 + 顶栏 + 内容区 / WorkspaceLayout：顶栏 + 三栏 + 底部）

---

## 七、地图集成方案

### 7.1 地图能力清单

| 能力 | 说明 |
|---|---|
| 底图 | 天地图（影像/矢量/地形），支持切换 |
| 缩放 | 滚轮缩放 + 缩放控件 |
| 平移 | 拖拽平移 |
| 指南针 | 指北针控件，点击复位 |
| 比例尺 | 比例尺控件 |
| 图层管理 | 图层列表面板，勾选显隐（设备层/事件层/航线层/区划层等） |
| 绘制工具 | 点/线/面/圆绘制，用于标绘和测量 |
| 测量 | 距离测量、面积测量 |
| 2D/3D 切换 | 二维地图与三维场景切换 |
| 无人机标注 | 实时位置 + 朝向 + 信息弹窗 |
| 航线展示 | 已飞航线（虚线）+ 计划航线（实线） |
| 事件标注 | 异常事件点标注 + 弹窗 |

### 7.2 地图组件封装

`components/MapContainer/` 对外提供统一 props：

```typescript
interface MapContainerProps {
  center?: [number, number];       // 中心点 [lng, lat]
  zoom?: number;                    // 初始缩放级别
  showLayerPanel?: boolean;         // 是否显示图层面板
  showDrawTools?: boolean;          // 是否显示绘制工具
  showMeasure?: boolean;            // 是否显示测量工具
  showCompass?: boolean;            // 是否显示指南针
  showScale?: boolean;              // 是否显示比例尺
  enable3D?: boolean;               // 是否启用3D
  baseMap?: 'vec' | 'img' | 'ter'; // 底图类型
}
```

### 7.3 天地图密钥配置

```env
# .env.development
VITE_TIANDITU_KEY=你的天地图开发密钥
VITE_TIANDITU_URL=https://api.tianditu.gov.cn/api
```

---

## 八、实时视频方案

### 8.1 视频组件能力

| 能力 | 说明 |
|---|---|
| 实时播放 | 无人机图传画面实时播放 |
| 多画面 | 支持1/4/9宫格切换，多路同时观看 |
| 截图 | 当前画面截图保存 |
| 录制 | 本地录制视频片段 |
| 全屏 | 单路全屏 |
| 云台控制 | 若支持，PTZ 方向控制（待确认） |
| 状态显示 | 分辨率/帧率/码率/信号强度 |

### 8.2 协议选型（待确认）

| 协议 | 适用场景 | 延迟 | 备注 |
|---|---|---|---|
| HLS (m3u8) | 通用直播，兼容性好 | 3-10s | 最简单，延迟较高 |
| HTTP-FLV | 低延迟直播 | 1-3s | 需要 flv.js |
| WebRTC | 实时通信 | <1s | 延迟最低，需信令服务器 |
| RTSP转码 | 无人机原生RTSP | 取决于转码 | 需媒体服务器转码 |

> **待确认**：无人机机库/图传系统输出的视频流格式和协议，据此确定播放器实现。

---

## 九、权限控制设计

### 9.1 权限模型

采用 **角色 + 资源权限** 两级模型：

```
用户 → 角色（如：管理员/巡查员/审核员/访客）
     → 权限点（页面级 + 组件级 + 操作级）
```

### 9.2 权限粒度

| 粒度 | 实现方式 | 示例 |
|---|---|---|
| 页面级 | 路由 meta.requiresAuth + 角色判断 | 普通用户不能访问工作台 |
| 组件级 | `<PermissionGuard code="xxx">` 包裹 | 审核员看不到"新增任务"按钮 |
| 操作级 | 按钮级 v-permission 指令 | 只有管理员能删除任务 |
| 数据级 | 接口返回数据按角色过滤 | 巡查员只看到自己的任务 |

### 9.3 初始角色定义（待确认）

| 角色 | 说明 | 可访问页面 |
|---|---|---|
| 系统管理员 | 全权限 | 全部 |
| 业务管理员 | 领域内管理 | dashboard + taskLists + workspace |
| 巡查员 | 执行任务 | dashboard + workspace（部分节点） |
| 审核员 | 复核归档 | dashboard + workspace（复核节点） |
| 访客 | 只读查看 | dashboard |

---

## 十、第三方系统嵌入预留

### 10.1 嵌入组件封装

`components/IframeEmbed/` 统一封装：

```typescript
interface IframeEmbedProps {
  url: string;                    // 嵌入页面 URL
  title?: string;                 // 标题
  height?: string | number;       // 高度
  showHeader?: boolean;           // 是否显示头部
  params?: Record<string, any>;   // URL 附加参数（如 token、任务ID）
  allow?: string;                  // iframe allow 权限（摄像头/全屏等）
}
```

### 10.2 通信机制

- **URL 传参**：通过 query 参数传递任务ID、token 等
- **postMessage**：预留父子页面通信通道，用于回调通知
- **登录态打通**：待确认第三方系统是否支持 SSO / token 透传

### 10.3 嵌入点登记（待补充）

| 编号 | 所在页面 | 第三方系统 | 嵌入方式 | 备注 |
|---|---|---|---|---|
| E1 | （待定义） | （待定义） | iframe | |
| E2 | （待定义） | （待定义） | iframe | |

---

## 十一、响应式适配

### 11.1 断点定义

```scss
$breakpoints: (
  xs: 480px,    // 手机竖屏
  sm: 768px,    // 平板竖屏
  md: 1024px,   // 平板横屏 / 小屏笔记本
  lg: 1366px,   // 标准笔记本
  xl: 1920px,   // 大屏显示器
  xxl: 2560px,  // 超宽屏 / 拼接大屏
);
```

### 11.2 适配策略

| 页面 | 适配策略 |
|---|---|
| Dashboard 大屏 | 以 1920×1080 为基准设计，使用 `transform: scale()` 等比缩放适配大屏；小屏降级为滚动布局 |
| TaskLists | 标准响应式，表格在小屏转为卡片列表，筛选栏折叠 |
| Workspace | 流程节点 Header 在小屏转为横向滚动；内容区自适应 |

### 11.3 大屏缩放方案

```typescript
// Dashboard 页面使用自适应缩放
// 以设计稿 1920×1080 为基准，计算 scale 值应用到根容器
function useDashboardScale() {
  const scale = ref(1);
  const update = () => {
    scale.value = Math.min(
      window.innerWidth / 1920,
      window.innerHeight / 1080
    );
  };
  onMounted(() => { update(); window.addEventListener('resize', update); });
  onUnmounted(() => window.removeEventListener('resize', update));
  return scale;
}
```

---

## 十二、接口约定（Mock 阶段）

后端接口就绪前，使用 Mock 数据并行开发。

### 12.1 接口前缀

```env
VITE_API_BASE_URL=/api
```

### 12.2 统一响应格式

```typescript
interface ApiResponse<T> {
  code: number;       // 0 成功，非0 错误码
  message: string;    // 提示信息
  data: T;            // 业务数据
}
```

### 12.3 核心接口清单（待后端确认）

| 模块 | 接口 | 方法 | 说明 |
|---|---|---|---|
| 认证 | /auth/login | POST | 登录 |
| 认证 | /auth/userInfo | GET | 获取当前用户信息+权限 |
| Dashboard | /dashboard/statistics | GET | 统计卡片数据 |
| Dashboard | /dashboard/tasks/recent | GET | 近期任务列表 |
| Dashboard | /dashboard/drones/online | GET | 在线无人机列表 |
| Dashboard | /dashboard/events/active | GET | 活跃事件列表 |
| Dashboard | /dashboard/workflow/progress | GET | 工单流转进度 |
| 任务 | /tasks | GET | 任务列表（分页+筛选） |
| 任务 | /tasks | POST | 新增任务 |
| 任务 | /tasks/:id | DELETE | 删除任务 |
| 任务 | /tasks/:id | GET | 任务详情 |
| 工作台 | /workspace/:field/:taskId/nodes | GET | 获取该任务的流程节点状态 |
| 无人机 | /drones/:id/stream | GET | 获取视频流地址 |
| WebSocket | /ws | — | 实时推送（无人机位置/告警/状态） |

---

## 十三、开发阶段规划

### Phase 1：项目脚手架（1-2天）

- [ ] Vite + Vue3 + TS 项目初始化
- [ ] 目录结构搭建
- [ ] 全局样式变量 + 主题配置
- [ ] Element Plus 引入 + 主题定制
- [ ] 路由 + Pinia + Axios 基础配置
- [ ] 3 个布局组件搭建（BasicLayout / DashboardLayout / WorkspaceLayout）
- [ ] 3 个页面空壳 + 路由连通
- [ ] 登录页 + 路由守卫基础版

### Phase 2：Dashboard 运行中枢（3-5天）

- [ ] 顶栏 + C1 用户切换弹窗
- [ ] 4个统计卡片组件
- [ ] 地图组件封装（天地图接入 + 基础控件）
- [ ] C2 无人机视频窗口组件
- [ ] 任务列表面板
- [ ] AI 识别结果面板
- [ ] 工单流转进度组件
- [ ] 底部流程节点条
- [ ] 大屏自适应缩放
- [ ] Mock 数据联调

### Phase 3：任务总览页（2-3天）

- [ ] 筛选栏组件
- [ ] 任务列表表格
- [ ] 新增任务弹窗
- [ ] 删除/批量操作
- [ ] 点击跳转工作台
- [ ] Mock 数据联调

### Phase 4：监管工作台框架（3-5天）

- [ ] WorkspaceLayout 顶栏 + Logo
- [ ] 动态流程节点 Header（按领域配置）
- [ ] 节点切换 + 动态组件加载
- [ ] 6个通用节点组件初版（C13-C18）
- [ ] 领域配置机制
- [ ] Mock 数据联调

### Phase 5：集成与优化（持续）

- [ ] WebSocket 实时推送接入
- [ ] 真实视频流协议对接
- [ ] 后端接口联调（替换 Mock）
- [ ] 权限系统完善
- [ ] 第三方系统嵌入对接
- [ ] 性能优化 + 多端适配验证

---

## 十四、待确认事项清单

| 编号 | 事项 | 状态 | 影响 | 优先级 |
|---|---|---|---|---|
| Q1 | 无人机视频流协议（HLS/FLV/WebRTC/RTSP） | ⏳ 待后续提供 | 视频播放器技术选型 | 高 |
| Q2 | 天地图密钥是否已申请 | ✅ 已申请 | 地图开发可启动 | 高 |
| Q3 | 用户角色与权限矩阵的完整定义 | ⏳ 待确认 | 权限系统实现 | 中 |
| Q4 | 各领域（林业/国土/交通/应急）的具体流程节点差异 | ⏳ 待确认（林业已确认7节点） | 工作台领域定制 | 中 |
| Q5 | 第三方嵌入系统清单与对接方式 | ⏳ 待确认 | iframe 组件实现 | 中 |
| Q6 | 后端接口文档交付时间 | ⏳ 待确认（接口已写好，晚些拿到） | 联调排期 | 高 |
| Q7 | 工作台参考图（页面3的设计参考） | ✅ 已提供 | 工作台视觉设计 | 中 |
| Q8 | 是否需要三维场景（Cesium）还是仅2D地图 | ⏳ 待确认 | 地图技术方案 | 中 |
| Q9 | 部署目标环境（内网/政务云） | ⏳ 待确认 | 构建配置 | 低 |
| Q10 | 项目正式名称 | ⏳ 待定 | 文档/标题/Logo | 低 |
| Q11 | 参考图中第6节点"现场核查与整改"上的红斜杠含义 | ✅ 已确认：去掉该节点，林业为6节点；其他领域节点数不固定 | 工作台流程节点数量 | 高 |

---

## 十五、下一步行动

1. **确认本文档**：你审阅后指出需要调整的部分（配色、组件、流程等）
2. **确认 Q11**：参考图中第6节点"现场核查与整改"的红斜杠是否表示取消该节点
3. **补充待确认事项**：Q1（视频协议，后续提供）、Q3（权限矩阵）、Q5（第三方嵌入清单）
4. **启动 Phase 1**：确认无误后，开始搭建项目脚手架代码（Vite + Vue3 + TS + 双主题 + 3布局 + 3页面空壳）
5. **天地图密钥**：开发时将密钥配置到 `.env.development`

---

*本文档为活文档，随项目推进持续更新。*
